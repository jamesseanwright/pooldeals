# General Guidelines and Tenets

- Only create, modify, or delete files under the `app` directory. Any other directory under the repository root (e.g. `src`, `AGENTS.md` etc) are not related to the PoolDeals product source but are for defining and configuring the CrewAI-based harness for autonomously building the product.
